import{useState}from'react';import{Navigate,useNavigate,Link}from'react-router-dom';import{Building2,ArrowRight}from'lucide-react';import{useAuth}from'../store/AuthContext';export default function Login(){const{user,login}=useAuth(),nav=useNavigate(),[form,setForm]=useState({identity:'admin',password:'Admin@123'}),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to="/dashboard"/>;const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(form.identity,form.password);nav('/dashboard')}catch(e){setError(e.response?.data?.message||'Unable to sign in')}finally{setBusy(false)}};return <div className="login"><div className="loginPanel"><div className="logo"><Building2/> PROP<span>Flow</span></div><p>Welcome back. Sign in to your workspace.</p><form onSubmit={submit}><label>Username or email<input value={form.identity} onChange={e=>setForm({...form,identity:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<div className="error">{error}</div>}<button className="primary full" disabled={busy}>{busy?'Signing in…':<>Sign in <ArrowRight size={17}/></>}</button></form><small>Demo: admin / Admin@123</small><Link to="/">← Back to home</Link></div><div className="loginArt"><p className="eyebrow">PROPFlow</p><h1>Maintenance, without the maintenance.</h1><p>Give residents, owners and technicians one focused system to move work forward.</p></div></div>}
