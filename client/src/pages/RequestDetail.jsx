import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import{api,unwrap}from'../services/api';import{useAuth}from'../store/AuthContext';import Badge from'../components/Badge';export default function RequestDetail(){const{id}=useParams(),{user}=useAuth(),[r,setR]=useState(null),[notes,setNotes]=useState('');const load=()=>unwrap(api.get('/maintenance/'+id)).then(x=>{setR(x);setNotes(x.resolutionNotes||'')});useEffect(load,[id]);if(!r)return <div className="center">Loading request…</div>;const status=async e=>{await unwrap(api.patch(`/maintenance/${id}/status`,{status:e.target.value}));load()};const save=async()=>{await unwrap(api.patch(`/maintenance/${id}/resolution`,{resolutionNotes:notes}));load()};return <><div className="pageIntro"><p className="eyebrow">REQUEST #{r.id}</p><h1>{r.title}</h1><p>{r.description}</p></div><div className="detailGrid"><section className="panel"><h3>Request details</h3><dl><dt>Property</dt><dd>{r.propertyName}</dd><dt>Unit</dt><dd>{r.unitNumber}</dd><dt>Priority</dt><dd><Badge value={r.priority}/></dd><dt>Status</dt><dd><Badge value={r.status}/></dd><dt>Reported by</dt><dd>User #{r.reportedBy}</dd><dt>Assigned technician</dt><dd>{r.assignedTo?`User #${r.assignedTo}`:'Unassigned'}</dd></dl></section><section className="panel"><h3>Resolution notes</h3>{['TECHNICIAN','ADMIN','OWNER'].includes(user.role)?<><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Add resolution notes…"/><button className="primary" onClick={save}>Save notes</button></>:<p>{notes||'No notes yet.'}</p>}{user.role==='TECHNICIAN'&&<><h3 className="space">Update status</h3><select value={r.status} onChange={status}><option>ASSIGNED</option><option>IN_PROGRESS</option><option>RESOLVED</option></select></>}</section></div></>}
